# Multigrid Attention（MGA）：层级 × 循环的线性注意力

**项目状态：v0.2 SparseRead 验收通过（2026-09-14）——线性化不掉点成立。**

## 一句话

把全注意力的完全图换成直径 2·log_b(n) 的树状层级图，配 top-m 稀疏读取；所有层级与循环共享同一算子。用循环深度和层级路由换注意力范围，得到对 n 线性、能精确检索、能零样本长度迁移的注意力。

## 方法（生产形态 v0.2）

输入 x ∈ R^{n×d}，块大小 b=16。共享算子：Θ（pre-norm transformer block）、Pool（learned cross-attn 池化）、Read。一个 **V-cycle**：

1. **平滑**：块内因果注意力（块折进 batch 维计算）
2. **限制**：每块池化出 1 个摘要（learned query cross-attn）
3. **递归**：同一 Θ 重复，直到顶层（≤ b 个），顶层全因果注意力
4. **延拓**：level-0 用 **SparseRead**（见下）；高层（≥1）用稠密 read
5. **后平滑**，残差融合；共 **T=2 个 cycle，权重全共享**

**因果规则**：摘要只能被"覆盖区间严格在过去"的 token 读取（cover_end 显式记账；任何注意力路径改动必须过泄漏测试：打乱末尾 10% 输入，前 90% logits 须 0 diff——已抓出 3 个真实泄漏）。

**SparseRead（level-0 读取，核心部件）**：
- **全局块共享选择**：每个 query 块（用前一块的均值 query，保因果）对全部摘要打分选 top-m（默认 64）。选择成本 (n/b)²，每步全局重打分 = 免费探索（MoBA 同族机制）
- **稀疏读取**：块内 token attend 这 m 个摘要 → O(n·m)
- **fine fanout**：m 中 top-mf（默认 4）展开为其覆盖的原始 token，token 级 cross-attn 直连（无效选择掩码）
- m → n/b 时退化为稠密 read（m 是精度-线性前沿的刻度尺）

**复杂度**：平滑+池化 O(n·b·d)；read O(n·m·d)。65k 时每 token 读 128 个 key vs 稠密 4097 个；全程对 n 线性。

## 核心结果（passkey exact-match）

| 实验 | 结果 | 意义 |
|---|---|---|
| 512 点火（dense / sparse） | 0.91–0.98 | 路由电路可训练 |
| 4096 冷启动（无课程） | ❌ 4000 步平坦 | 点火阈值现象（见"根因"） |
| **512→4096 课程迁移（dense）** | 零样本 **0.9609** → 0.9922 | 电路尺度可迁移 ×8 |
| **512→4096 课程迁移（sparse m=64）** | **0.9609** | **线性化不掉点 ✓（用户硬标准）** |
| **sparse m=64 @16k 零样本** | **0.8984**，depth [0.892, 0.897, 0.885, 0.917] | **反超 dense 的 0.7422**（16× 更少 key）；深位衰减治愈 |
| dense @16k 零样本 | 0.7422，深位 0.58–0.72 | 路由对比度稀释（对照） |
| cycles_exact @4096 | [0.031, 1.0] | 第二个 cycle 做几乎全部检索 |
| T 消融零样本 @4096 | T=1: 0.008 / T=2: 0.97 / T=4: 0.09 | T 须匹配训练；T=4 把解破坏（过冲） |
| T=1 课程救援 | 0.9219 @4096 | 循环是容量放大器，不是魔法 |
| **MQAR 16对×16查询 @512（128 点火迁移）** | **1.0000**，cycles [0.953, 0.953] | 阶梯点火通杀；两 cycle 均衡分担 |
| **MQAR @4096（512 零样本迁移）** | **1.0000（step 0，零训练步）** | 迁移证据的终极形态 |
| sparse @65k | 显存 35G > 32G，差一点（`--bs 4` 或 `--bf16` 可解） | scaling 门槛基本跨过 |
| **LM @4096 halo（v0.4 收官）** | **bpc 1.5557**（digit 0.685），posloss 平坦 [1.60–1.67] | **反超参数匹配 full-d2 的 1.664**；边界饥渴消除 |

基线对照：full @4096 = 0.957（上界）；local = 0.000（判别性成立）。

## 真实语言首战（enwik8 char-LM，2026-09-15）

**结果**（15k 步，bs 32，n=4096）：full bpc **1.243**（digit 0.745）；mga cold bpc **1.745**；mga 课程 bpc **1.736**；local bpc ~2.0。

**三个不被"不理想"掩盖的事实**：
1. **冷启动点火成功且与课程打平**——SNR 定式预言兑现：稠密监督下点火问题天然消失。"长度 vs SNR 谁是自变量"裁决 = **SNR**。
2. **金字塔在真实文本上做工**（1.745 ≪ local 2.0）。
3. 三方曲线终点均未收敛（~20 epoch）。

**差距根因分析**（嫌疑排序）：① **容量 3.3×**（full 6.32M vs mga 1.91M；train loss 0.78 vs 1.19 是容量签名——LM 买统计容量，不同于检索买电路）② 16-token 块对 LM 上下文充分性苛刻（~2-3 词/块，一切过有损摘要）③ 稀疏 read 的 LM 税（m=64/257）④ 预算。**架构 claim 从来不是同尺寸打赢全注意力，而是可负担地 scale**——但需 C1 判决书证实差距构成。

**对照矩阵（进行中）**：C1 full@depth2（≈1.6M，参数匹配判决）；C2 mga m=257（稀疏税隔离）；C3 mga kq=4（金字塔带宽）；C4 双方 30k 步。诊断工具：`posloss_mod_b`（loss 按块内位置分桶——定位 gap 在块边界[跨块饥渴]还是均匀[容量]）。

**C1 判决（2026-09-15）：full@depth2 bpc 1.6644**（digit 0.663，train 1.11）。**0.5 bpc gap 分解：~0.42 = 容量，~0.08 = 真实架构残差**（mga 1.91M 还略多于 full-d2 的 1.6M）。残差药方的选择依赖 `posloss_mod_b` 定位：块边界隆起 → 跨块饥渴 → 带宽（kq）/读取是对的；均匀 → 容量/表达，带宽白搭。full-d2 的 posloss 平坦（1.56–1.66 无边界结构，符合预期）。**下一步**：D0 = mga posloss（`--eval_only` 零成本）→ C3/C2 → mga@16k 零样本 bpc（真实文本外推，封顶图）。

**D0 解剖（2026-09-15）**：残差**全部**长在每个块的前 4 个 token（pos0=2.226 vs 块中 ~1.65，16k 时放大到 3.55）——块内因果注意力使块首 token 局部上下文为零，只能靠摘要续命。**T1 halo 平滑**（keys = 前一块 + 本块，2b 因果窗口）一击消除：**bpc 1.745 → 1.5557，反超 full-d2，posloss 平坦**。注意：halo  ≠ 被否决的 shifted blocking——边界固定只是开窗，泄漏测试 0 diff。V 系列检验（SNR 纯正性/迁移前沿/迁移稳健性）因用户无疑而取消。

## 标度、KV cache 与推理形态（2026-09-15，side-chat 核定后入档）

**每 token 训练前向成本**（d=256, b=16, T=2, m=64）：平滑+后平滑 ~26d²、halo ~1d²、池化 ~2d²、SparseRead ~5d²（/cycle），选择打分 n·d/b²（唯一增长项）→ **~66d² ≈ 4.3M MACs/token，对 n 近似常数**。对照 full-d2 = 24d²+4nd（4096: 5.8M；65k: 274M）、full-d8（65k: 1.1G）。交叉点 n≈2.5k。**渐近四维度全部线性/常数且已实证到 65k**：FLOPs、显存（无 O(n²) 结构）、KV cache、生成成本。

**选择的诚实账（总量 O(n + n²/b²)）**：read 与显存严格线性；选择打分 (n/b)² 在 65k 占 1.5%、1M 占 ~20%、**~4.3M 与线性部分打平**（n ≈ 66·b²·d）。选择矩阵 4M 名义 125GB，chunked top-k 流式打分可压到几 GB；4M 外由**层级选择**（顶层摘要预筛）恢复近线性。**b² 是 256× 延期券不是免单券**——把二次增长起点从 16k 推到 4M。论文 claim 写法：≤100k 区间工程线性成立（选择占比 <2%），更长序列由层级选择恢复。（核定勘误：side-chat 中 1M 占比 26% 实为 ~20%、4M >100% 实为 ~50:50，结论不变。）

**KV cache 与流式生成（原理成立，工程未做）**：
- 块内因果 + halo 只看前一块 + cover_end + 只读严格过去 ⇒ **块填满后其在所有 cycle/层级的表示终态不变**——金字塔天然流式可建；增量 V-cycle 调度器（块填充状态机 + 缓存管理）是纯工程，零架构改动。
- 缓存 = level-0 状态 T·n·d + 金字塔摘要 n·d/(b-1) ≈ **T·n·d，与 T 层 transformer KV cache 同阶**。
- 生成每 token 成本 O(1) 与 n 无关：块内 O(b) + sparse read O(m) + fanout O(mf·b) + 选择摊销 O(n·d/b²)（65k 时 ~65k MACs/token，可忽略）。**金字塔同时是缓存与索引（B 树 vs 链表）**——长程检索生成时 O(log n) 命中不扫描；SnapKV/Quest 是事后给 KV 做摘要，我们是架构原生。

**短板（诚实清单）**：①小 n 常数打不过 FlashAttention（4096 以下折叠/gather/小 kernel 拖累，战场定在 ≥16k）；②T=2 是硬乘数（T=1@LM 消融是性价比最高的后续实验）；③~4M 后选择需层级化（远期）；④共享权重的容量税（同 FLOPs 表达力被 ~10 个角色均摊，unshared/adapter 问题 LM 上未回答）。

## 根因与理论认识（论文素材）

1. **点火阈值（SNR 定式，2026-09-14 定稿）**：冷启动失败是**路由梯度信噪比**问题，不是架构能力。高原 = 边缘分布解的鞍部平面（ln(K) 平台）；深谷 = 检索电路盆地；逃逸速度 ∝ **路由梯度 SNR ≈ 监督密度 / 每 read 摘要数**：512→33 个（阈值边缘），4096→257 个（阈值下），128→9 个（远超阈值，弹道点火）。梯度 SNR 低于噪声地板时，SGD 退化为带微弱偏置的随机游走（首通时间问题），故点火总是顿悟式跳变。树深、带宽、bf16、lr、CUDA 全部排除（R2/R3/g 证据链）。**配方一句话：在看得见谷底的地方找到它，然后住在里面走遍所有尺度**（共享算子保证 128 与 4096 的解是参数空间同一点，证据 = 零样本 0.96）。待验预言：稠密监督（LM）下冷启动可能直接点火——B1a 裁决"长度是自变量还是 SNR 是自变量"。
2. **课程点火（标准配方）**：小尺度点火（512, ~3000 步, bs 64, lr 1e-3）→ `--resume_weights_only` 到目标 n（共享算子参数与 n 无关，优化器/余弦重置）。这是配方，也是可迁移性证据。
3. **零样本长度迁移**：512 的电路在 4096/16k 零训练步工作（0.96/0.90）；MQAR 上更达 **1.0 零样本 @4096**——迁移证据的终极形态。**纪律（L3 教训）**：迁移后在目标尺度以常规 lr 续训会把电路先拆掉（灾难性遗忘，step 400 归零）——生产流程中迁移评估一律取 `--eval_only` 零样本值；目标尺度续训需极小 lr 或不训。
4. **循环的证据与边界**：cycles_exact [0.03, 1.0] 证明迭代做工；T=4 过冲 [0, .938, .375, 0] 证明算子在解处无不动点——"T 测试时旋钮"朴素形式死亡（复活需随机 T 训练出吸引不动点，已搁置）。
5. **截断洞察（用户）**：硬截分不是根本问题——模型只需识别截断，即可经层级+迭代跨块重建联系（shifted blocking 因此有害无益）。
6. **理论边界**：可证直径定理（任意两 token 依赖路径 ≤ 2T·log_b n 跳）；不可证收敛率（softmax 非线性，multigrid 经典证明不适用）——用经验收敛曲线代替。

## 否决清单（全部有实验依据）

| 方案 | 判决 | 依据 |
|---|---|---|
| AMR 树下降（v0.2 初版） | ❌ | beam myopia：scorer 学习被自身选择绑架；ST 梯度 / Gumbel 退火 / dense 接手均救不回（A1 仅 0.14） |
| shifted blocking | ❌ 毒性 | 512 bisect 显著拖慢（0.46 vs 0.89 @1k） |
| per-cycle 出口损失 | ❌ 毒性 | 同上（0.48 vs 0.89）；强迫单 cycle 可解与分工冲突 |
| posxattn（新/旧实现） | ❌ 驳回 | 用户决定；新 token 索引实现有混叠嫌疑 |
| poolaux（BOW 辅助损失） | ❌ 无效 | 只教 pool"写什么"，不建立 read"怎么读"（R6 平坦） |
| bf16 嫌疑 | ✅ 清白 | R3 fp32 同样失败 |
| 不动点训练（T-dropout） | ⏸ 搁置 | 用户否决（T=4 过冲是其存在理由，留档） |

## 文献定位

| 路线 | 代表工作 | 层级？ | 循环共享权重？ |
|---|---|---|---|
| 层次/多尺度注意力 | H-Transformer-1D (2021)、FMMformer (2021)、Hourglass、Swin | ✅ | ❌ 每层权重不同，单 cycle |
| 深度循环/权重共享 | Huginn (NeurIPS 2025)、ByteDance Ouro、Mixture-of-Recursions | ❌ 固定分辨率 | ✅ 深度方向 |
| 序列循环/记忆 | Block-Recurrent Transformer、RMT、Infini-attention | ❌ | ✅ 沿序列 |
| 工业稀疏注意力 | DeepSeek NSA、Kimi MoBA | 一层粗+细 | ❌ |

**层级 × 循环的交叉格是本工作占据的位置**；SparseRead 的选择机制与 MoBA/NSA 同族，差异在"摘要树上的块路由 + fine fanout + 跨尺度共享算子"。命名注意：H-Transformer-1D 已用过 "multigrid"，投稿建议 **V-cycle Transformer**。

## 下一步

1. **S4 收尾**：65k eval 用 `--bs 4` 或 `--bf16` 降显存跨过 35G→32G
2. **m 前沿图**：m ∈ {16, 32, 64, 128} × n ∈ {4096, 16384} 的 exact 曲面（`--eval_only` 零训练）
3. **MQAR/copying 泛化**（sparse 配方）；**循环必要性压力测试**（MQAR/多跳上 T=1 vs T=2——passkey 已证非必需）
4. **真实语料**：enwik8/PG-19 + 课程配方，LM loss 主指标；fixed-FLOPs Pareto 前沿（H3）；对照需含匹配预算的强稀疏基线
5. **论文框架**：method（V-cycle + SparseRead + 共享算子）→ 点火阈值与课程配方 → 零样本迁移 → 循环证据与 T 分析 → m 前沿 → 65k scaling

## 工程笔记

- 折叠批在 CUDA 上超 grid y 上限（65535）→ `FOLD_CHUNK=8192`；CrossAttn 手工 matmul（免疫 SDPA 后端差异）
- 远程 AutoDL；`sweep.sh`（per-GPU worker 串行）；结构化日志 `runs/*.jsonl`（含 gnorm/emb_n/depth_exact/cycles_exact）
- 关键 checkpoint：`runs/sp512.pt`（512 点火）、`runs/sp4096.pt`（4096 上岗）
- 词汇表分工（`mga/data.py`）：passkey/copying fillers=18-29；MQAR fillers=26-29、keys=10-25 **无放回**采样（曾因 8 key 有放回 → 同 key 不同 value 的矛盾标签 → loss 冻结于 ln(10)=2.30，已修复并验证一致性）
- **MQAR 战役全记录（2026-09-14，搁置→满分）**：①8 key 有放回 → 矛盾标签冻结于 ln(10)（数据 bug，已修）；②修复后仍平 → 我曾误判"结构性障碍"，对照实验纠正：passkey 在同预算（400 步/d=128）同样平——真问题是点火预算不是结构；③远程 lr 1e-3 尖峰（gnorm 940）打断进度，lr 5e-4 治好；④**SNR 处方**：n=128（9 摘要、pair 间距 7、`--nqueries 16`）弹道点火（digit 0.982/exact 0.80，cycles [0.438, 0.828]）；⑤**阶梯迁移 128→512：eval exact 1.0000，cycles [0.953, 0.953]**。教训：对照组的预算必须与判决匹配；循环必要性探测后续补 T=1 对照
